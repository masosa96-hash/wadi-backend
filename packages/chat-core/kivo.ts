export function pensar(input: string) {
  // Kivo desarma la idea, encuentra estructura, arma estrategia
  return {
    esencia: input.trim(),
    plan: [
      "Entender el problema de raíz",
      "Dividirlo en partes accionables",
      "Elegir el camino más directo",
    ],
    contexto: "análisis silencioso ejecutado",
  };
}
